
jwt {
    secret = ${?JWT_SECRET}
    issuer = ${?JWT_ISSUER}
    audience = ${?JWT_AUDIENCE}
    realm = ${?JWT_REALM}
    expiry = ${?JWT_TOKEN_EXPIRY}
}


aws {
     accessKey = ${?AWS_ACCESS_KEY_ID}
     secret = ${?AWS_SECRET_ACCESS_KEY}
}
